import { StyleSheet } from "react=-native";

const styles = StyleSheet.create({
  tile: {
    borderWidth: 2,
    borderColor: "#000",
    padding: 10,
    borderRadius: 10,
    borderRadius: 10,
    marginVertical: 10,
    alignItems: "center",
    justifyContent: "center"
  },
   num: {
    color: "#000",
    fontSize: 22
  }
});

export { styles }
